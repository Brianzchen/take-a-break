import React from 'react';

const style = {
  display: 'inline-block',
  margin: '11px',
  borderLeft: '1px solid #ccc',
};

const VerticalRule = () => (
  <div style={style} />
);

export default VerticalRule;
